service: aquarius
frameworkVersion: '2 || 3'

plugins:
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 5000

provider:
  name: aws
  runtime: python3.8
  lambdaHashingVersion: '20201221'
  httpApi:
    cors: true
  environment:
    AWS_ACCESS_KEY: fakeAccessKeyId
    AWS_SECRET_ACCESS_KEY: fakeSecretAccessKey
    AWS_REGION: us-east-1
    DB_URL: http://localhost:8000

functions:
  putJob:
    handler: handler.put_job
    events:
      - httpApi:
          path: /api/job
          method: post

  updateJob:
    handler: handler.update_job
    events:
      - httpApi:
          path: /api/job/{id}
          method: put

  getJobs:
    handler: handler.get_job
    events:
      - httpApi:
          path: /api/job
          method: get

  getJob:
    handler: handler.get_job_by_id
    events:
      - httpApi:
          path: /api/job/{id}
          method: get

  deleteJob:
    handler: handler.delete_job
    events:
      - httpApi:
          path: /api/job/{id}
          method: delete
